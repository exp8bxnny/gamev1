event_inherited();

alarm[0] = 0.5 * game_get_speed(gamespeed_fps);

show_text = true;

text_index = 0;
displayText = ["?: Don't cry now...you did this...", 
			   "?: You chose this path, so walk it."];